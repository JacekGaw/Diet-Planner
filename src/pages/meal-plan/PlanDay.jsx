import React from 'react';

const PlanDay = ({index}) => {
    return (
        <div>
            <header>
                <p>{index}</p>
            </header>
        </div>
    )
};

export default PlanDay;
